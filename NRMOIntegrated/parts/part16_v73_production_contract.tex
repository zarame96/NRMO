% =================================================================
% Part XVI: NRMO v7.3 — Production Contract Integration
%   Reconciles the located v7.3 operational-source originals (primary
%   v7.3 evidence, NRMOIntegrated/source/v7.3/original/, 2026-09-21)
%   against the v7.2 / v7.2.1 / Normative Canon baseline (superior
%   normative authority). DecisionCompass implementation-scope
%   documentation is corroborating/conformance evidence only, not a
%   source of v7.3 requirements. Does not amend the Normative Canon.
%   See NRMOIntegrated/docs/V7_3_DIFFERENTIAL_TABLE.md for the full
%   row-by-row reconciliation this Part summarises, and
%   NRMOIntegrated/docs/V7_3_ORIGINAL_SOURCE_DISCOVERY_RECORD.md for
%   the discovery and authenticity record.
% =================================================================

\part{NRMO v7.3 --- Production Contract Integration}
\label{part:v73_production_contract}

\chapter{Provenance and Status of This Part}
\label{ch:v73_provenance}

\nrmobox{Provenance and authority status --- read before use (updated 2026-09-21)}{
\textbf{The original ``NRMO SYSTEM v7.3'' specification text has been
located and is stored in this repository.} DecisionCompass Issue
\#120 records that such a document (\texttt{NRMO SYSTEM\_7.3.md} and
\texttt{NRMO\_SYSTEM\_v7\_3\_PATCH.md}) was supplied 2026-09-15; both
files were subsequently supplied directly by the Human Sovereign,
authenticity-verified, and stored byte-for-byte unmodified at
\texttt{NRMOIntegrated/source/v7.3/original/} (SHA-256 recorded in
\texttt{SOURCE\_MANIFEST.md} in that directory). Full discovery record
and authenticity assessment:
\texttt{docs/V7\_3\_ORIGINAL\_SOURCE\_DISCOVERY\_RECORD.md}. \\[0.5em]
\textbf{This Part is not, and does not claim to be, a verbatim
transcription of that original}; it is a reconciliation, restructured
and cross-referenced against \texttt{NORMATIVE\_CANON.md} and the v7.2
monograph's chapter structure. Where this Part's content is confirmed
against the located original, it is marked \textbf{ORIGINAL-CONFIRMED}
with a citation to the specific original section. Where content has no
original-text precedent but was explicitly adopted by the Human
Sovereign as new v7.3 content on 2026-09-21, it is marked
\textbf{HUMAN-SOVEREIGN-ADOPTED TRACE EXTENSION} (currently: part of
the DecisionRecord trace schema only, \S\ref{ch:v73-trace-schema}).
One item (StrongEngine internal candidate-generation diversity,
\S\ref{sec:v73-strongengine-surface}) has no original-text precedent at
all and was explicitly placed \textbf{outside v7.3 normative scope}
by Human Sovereign decision (2026-09-22, not a rejection, preserved as
a future/independent proposal, \emph{not} a v7.3 publication blocker),
clearly separated from the original-confirmed presentation-layer
content in the same section. \textbf{No content in this Part amends
\texttt{NORMATIVE\_CANON.md}.} Any apparent tension with the Canon is
resolved in the Canon's favor; see
\S\ref{sec:v73-typezero-disambiguation} and
\S\ref{sec:v73-ruin-disambiguation} for the two cases this build found.
\\[0.5em]
\textbf{Decision history (preserved, not erased)}: before the original
was located, 5 of 6 then-SOURCE-PENDING items were independently
reconstructed from surviving secondary evidence and explicitly
\textbf{ADOPTED} by Human Sovereign decision on 2026-09-21 (see
\texttt{docs/V7\_3\_ADOPTION\_RECORD.md}, preserved unmodified as
historical record). The original, located later the same day, confirmed
\textbf{Items 1--4} (\S\ref{sec:v73-hold-state-machine} empty-set rule,
\S\ref{ch:v73-norn} responsibilities and drift vocabulary,
\S\ref{sec:v73-decision-procedure}) and \textbf{the original-confirmed
portion of Item 6} (\S\ref{ch:v73-trace-schema}'s response-content
fields) at or near verbatim --- a notable but coincidental convergence
for those items, not a claim that the reconstruction effort somehow
produced or was equivalent to the original. \textbf{Item 6's additional
trace-persistence fields remain a separately adopted post-hoc
extension} with no original-text precedent (\S\ref{ch:v73-trace-schema});
they are not covered by this verbatim confirmation and must not be read
as if they were. \\[0.5em]
This build's overall \textbf{PROVISIONAL} status (this notice, the
frontmatter notice, and the title page) was \textbf{unchanged} by the
original's discovery or by this Part's update to reflect it --- removing
PROVISIONAL status was, at that time, a separate Human Sovereign
decision, not made here. \textbf{That separate decision has since been
made}: on 2026-09-22, on the basis of
\texttt{docs/V7\_3\_PUBLICATION\_READINESS.md}'s readiness assessment,
the Human Sovereign removed the build's PROVISIONAL status. That same
day, the branch carrying this build was separately merged into
\texttt{main} (PR \#16, merge commit
\texttt{cb5d2b406d4c302b1e092c619a7cf716973d1e71}), by its own separate
Human Sovereign approval. Current status is \textbf{FINAL --- MAIN
INTEGRATED} (see \texttt{docs/V7\_3\_PUBLICATION\_DECISION.md} for both
decisions).
}

\section{Relationship to DecisionCompass}
\label{sec:v73-decisioncompass-relationship}

Per the Founding Charter and \texttt{NORMATIVE\_CANON.md} \S11
(Conformance Rule), updated to reflect the 2026-09-21 original-source
discovery, the authority/evidence order is:

\begin{center}
\texttt{Human Sovereign} $\to$
\texttt{NORMATIVE\_CANON.md / v7.2} $\to$
\texttt{located v7.3 operational originals} $\to$
\texttt{this Part (Integrated v7.3 reconciliation)} $\to$
\texttt{DecisionCompass implementation} $\to$
\texttt{conformance evidence}
\end{center}

This Part is written to precede and govern DecisionCompass's v7.3
implementation, not to certify or adopt it, and NRMO theory is never
derived from DecisionCompass's implementation (Canon \S11). The located
v7.3 originals (\texttt{NRMOIntegrated/source/v7.3/original/}) are the
\textbf{primary} v7.3 operational-source evidence for this Part, superior
to any DecisionCompass documentation. Prior to their discovery,
DecisionCompass's implementation-scope document
(\texttt{docs/NRMO\_V7\_3\_IMPLEMENTATION\_SCOPE.md}) was the best
available secondary evidence and was treated, per
\texttt{NORMATIVE\_CANON.md} \S9 and \S11, as informative, not
self-authorizing --- that treatment and its outcome are preserved as
history in \texttt{docs/V7\_3\_ADOPTION\_RECORD.md} and
\texttt{docs/V7\_3\_ADOPTION\_PROPOSAL.md} (see their superseding
notices), but is no longer this Part's active evidentiary basis for
Items 1--4 and Item 6's original-confirmed portion. DecisionCompass's
own conformance matrix (\texttt{docs/nrmo\_v7\_3\_conformance.json}) and
runtime/tests remain \textbf{implementation/conformance evidence only}
(Table~\ref{tab:v73-classification-summary}, row category
\texttt{IMPLEMENTATION-ONLY}), never a source of v7.3 requirements; see
\S\ref{sec:v73-decisioncompass-conformance-summary} and
\texttt{docs/V7\_3\_DECISIONCOMPASS\_CONFORMANCE\_REEVALUATION.md}.

\chapter{Authority, Mode, and Lifecycle Model}
\label{ch:v73-authority-mode}

\section{Authority Hierarchy (restated, unchanged)}
\label{sec:v73-authority}

\begin{center}
\texttt{Human Sovereign} $\to$ \texttt{Vision (human-owned)} $\to$
\texttt{NRMO (governance/admissibility)} $\to$ \texttt{A\_allowed} $\to$
\texttt{StrongEngine \(\Omega\) Full (search/rank only)} $\to$
\texttt{recommendation} $\to$ \texttt{Human Sovereign (final)}
\end{center}

This is \texttt{NORMATIVE\_CANON.md} \S2 restated with the admissible
set made explicit as an intermediate stage; it introduces no new
authority claim. The strict invariant
$a_t \in A_t$ (Canon \S2.3, \S8) is unchanged.

\section{Four-Layer Mode/State Model}
\label{sec:v73-four-layer}

Four state axes must be represented and traced \emph{separately} and
must never be flattened into a single enum:

\begin{enumerate}
\item \textbf{Operational Mode}: \texttt{NORMAL | SAFE | VENTURE |
MISSION} --- Canon \S3.1, unchanged.
\item \textbf{Context Override}: \texttt{TRAINING(A--F,E+) | HARE |
NONE} --- Canon \S3.2--\S3.3, unchanged.
\item \textbf{Type ZERO Internal Gating State} --- see
\S\ref{sec:v73-typezero-disambiguation}; historical labels, disambiguated.
\item \textbf{Lifecycle State}: \texttt{ACTIVE | HOLD | EXIT |
SAFE\_EXIT} --- Canon \S3.4, unchanged.
\end{enumerate}

\noindent
The $A_{\mathrm{allowed}}$ extension \texttt{MISSION-DEFENSE =
active|inactive} (Canon \S3.6, Section~\ref{sec:mission-defense-c2}) is
an extension flag on axis (1)'s MISSION profile, not a fifth axis.

\subsection{Disambiguation: Type ZERO Internal Gating State vs Operational Mode}
\label{sec:v73-typezero-disambiguation}

\nrmobox{ORIGINAL-CONFIRMED + CLARIFIED --- terminology-collision risk resolved}{
\textbf{Finding (\textbf{ORIGINAL-CONFIRMED})}: the located original
directly confirms a ``Type ZERO Mode'' axis using the labels
\texttt{CORE|VENTURE|MISSION|SHUTDOWN} --- its \S6 response format
lists \texttt{Type ZERO Mode: CORE / VENTURE / MISSION / SHUTDOWN} as a
line item separate from \texttt{Operational Mode: NORMAL / SAFE /
VENTURE / MISSION}, and its \S8.3 gives Type ZERO Modes a dedicated
definition with its own \texttt{IRREV}/\texttt{VOL}/\texttt{LOAD}
transition triggers. Section~\ref{sec:type-zero-modes-c2} already
preserves exactly this 4-tuple as an explicit \emph{historical,
superseded} table --- superseded specifically \emph{as the Operational
Mode taxonomy} (\texttt{NORMATIVE\_CANON.md} \S5). Reusing identical
labels (\texttt{MISSION}, \texttt{VENTURE}) for two different axes ---
confirmed by the original's own side-by-side \S6 listing --- creates a
serious readability and implementation-drift risk: a trace value
\texttt{MISSION} would be ambiguous between ``Operational Mode =
MISSION'' and ``Type ZERO internal gate = MISSION'' without
qualification. \\[0.5em]
\textbf{Resolution (\textbf{CLARIFIED}; Canon-priority reading, no
Canon amendment)}: Canon \S5 supersedes this label set only as the
Operational Mode taxonomy. It does not forbid Type ZERO from retaining
an internal, non-actuator (Canon \S6) gating-state vocabulary that
happens to reuse historical labels, \emph{provided it is never exposed
or logged without an explicit \texttt{type\_zero\_gate} qualifier
distinct from \texttt{operational\_mode}}. Any implementation or
document that prints a bare \texttt{MISSION} without stating which axis
it belongs to is non-conformant. This qualifier discipline is this
Part's own documentation/trace-schema obligation, not itself stated
verbatim in the original, and not a theory change.
}

Required trace discipline going forward: every occurrence of this
label set outside Section~\ref{sec:type-zero-modes-c2} itself must be
qualified as \texttt{type\_zero\_gate.\{CORE|VENTURE|MISSION|SHUTDOWN\}}
to prevent confusion with \texttt{operational\_mode.\{NORMAL|SAFE|VENTURE|MISSION\}}.

\section{HOLD State Machine}
\label{sec:v73-hold-state-machine}

\nrmobox{Correction recorded during self-audit (LOOP 6)}{
An earlier draft of this section classified the transition table below
as \texttt{ADDED / SOURCE-PENDING}, i.e.\ as having no direct v7.2
textual precedent. Re-reading Chapter~\ref{ch:theoretical-invariants}
(\S\ref{sec:inv-state-space}) during self-audit found that this exact
five-transition table already exists there, verbatim, as part of the
v2.0 theoretical invariants (\textbf{UNCHANGED} status in that
chapter's own terms). The classification below is corrected
accordingly. This correction is recorded rather than silently applied,
per the evidence-discipline requirement governing this Part.
}

\texttt{NORMATIVE\_CANON.md} \S3.4 establishes the lifecycle-state
family; Chapter~\ref{ch:theoretical-invariants} \S\ref{sec:inv-state-space}
already provides the transition table itself. The following table is
therefore \textbf{UNCHANGED} (restated for local readability in the
v7.3 context, not new content):

\begin{table}[ht]
\centering
\small
\begin{tabularx}{\linewidth}{lXX}
\toprule
\textbf{Transition} & \textbf{Trigger} & \textbf{Authority required} \\
\midrule
\texttt{ACTIVE $\to$ HOLD} & Human intervention required (e.g.\ empty
$A_{\mathrm{allowed}}$, materially insufficient information for a
major/irreversible candidate) & NRMO governance signal; no human
action needed to \emph{enter} HOLD \\
\texttt{ACTIVE $\to$ EXIT} & Planned termination (mission
success/exit criteria met) & Per the applicable MISSION exit criteria \\
\texttt{ACTIVE $\to$ SAFE\_EXIT} & Emergency termination & NRMO
governance signal (ruin-boundary or SHUTDOWN-equivalent trigger) \\
\texttt{HOLD $\to$ ACTIVE} & Resume & \textbf{Explicit Human Sovereign
approval only} (Canon \S2.1) \\
\texttt{HOLD $\to$ SAFE\_EXIT} & Escalation (HOLD condition
deteriorates or times out unresolved) & NRMO governance signal \\
\bottomrule
\end{tabularx}
\caption[HOLD state machine]{HOLD lifecycle transitions. UNCHANGED:
verbatim restatement of Chapter~\ref{ch:theoretical-invariants}
\S\ref{sec:inv-state-space} (v2.0 theoretical invariant, preserved
through v7.2), read together with Canon \S3.4.}
\label{tab:v73-hold-transitions}
\end{table}

\noindent
See also \S\ref{sec:hold-refinement} (\texttt{HOLD\_WITH\_OBSERVATION
| HOLD\_WITH\_SHADOW | HOLD\_WITH\_EXPIRY}) for the pre-existing
operational refinement of the single abstract \texttt{HOLD} state used
above; that refinement is orthogonal to this table and is unchanged by
it.

\paragraph{Empty admissible set (\textbf{ORIGINAL-CONFIRMED}).} If
$A_{\mathrm{allowed}}$ (equivalently
$A_t$) is empty, the execution layer returns \texttt{HOLD} and
generates \emph{no} execution candidate. This follows directly from
Canon \S8 (execution may never expand $A_t$ on its own authority): if
governance has narrowed $A_t$ to $\emptyset$, no admissible candidate
can exist by construction, and any output claiming otherwise would
violate $a_t \in A_t$. Confirmed at near-verbatim precedent in the
located original, \S8.4 (original-language text preserved unmodified in
that file; not reproduced here to avoid embedding untypeset CJK text in
this Part) --- see
\texttt{NRMOIntegrated/source/v7.3/original/NRMO SYSTEM\_7.3.md} and
\texttt{docs/V7\_3\_ORIGINAL\_SOURCE\_DISCOVERY\_RECORD.md} \S6 item 1
for the quoted original text.
Independently reconstructed and adopted by explicit Human Sovereign
decision, 2026-09-21, before the original was located (see
\texttt{docs/V7\_3\_ADOPTION\_RECORD.md}, item 1) --- that adoption
decision is preserved as historically valid and is not superseded by
this confirmation.

\chapter{Norn: Observation Responsibilities}
\label{ch:v73-norn}

\texttt{NORMATIVE\_CANON.md} \S7.1 already establishes canonical Norn
as an interpretive governance perspective that is \emph{not} the
sovereign, not a decision-maker, not StrongEngine, not an execution
engine. The following itemized responsibility list is
\textbf{ORIGINAL-CONFIRMED}: near-verbatim match to the located
original's \S4.10 ``Core Responsibilities'' 1--4 (branch record /
procedure audit / drift detection / passive observation --- original-
language labels preserved unmodified in the source file, not
reproduced here) and its two-sided upstream/downstream placement
description. Independently reconstructed and adopted by explicit Human
Sovereign decision, 2026-09-21, before the original was located (see
\texttt{docs/V7\_3\_ADOPTION\_RECORD.md}, item 2) --- preserved as
historically valid, not superseded by this confirmation.

\section{Required Observable Responsibilities}

\begin{itemize}
\item Branch record: proposed / admissible / selected / rejected
alternatives.
\item Procedure audit: whether the ordered evaluation of
\S\ref{sec:v73-decision-procedure} was actually followed by the
runtime, including detection of missing or reordered steps.
\item Conceptual-drift observation, using the descriptive
classification \texttt{DRIFT | SIGNAL | WITHIN\_OWN\_RANGE |
INSUFFICIENT\_HISTORY} (\textbf{ORIGINAL-CONFIRMED} --- exact label
match to the located original's \S4.10 ``Handling of Change'' table
(original-language heading preserved unmodified in the source file);
see
\texttt{docs/V7\_3\_ORIGINAL\_SOURCE\_DISCOVERY\_RECORD.md} \S6 item 3;
descriptive only; \textbf{no Norn-owned decision threshold is
permitted} --- ``audit has no scale'', directly entailed by Canon
\S7.1 + \S8).
\item Passive observation: stagnation, opportunity loss, adaptation
delay.
\item Post-decision, one-way observation that cannot alter the
decision already made.
\item Upstream observation immediately after normalized World
Model/Observation, where the runtime makes this feasible.
\end{itemize}

\section{Explicit Prohibitions (restated from Canon, made explicit)}
\label{sec:v73-norn-prohibitions}

Canonical Norn \textbf{must not}: veto; execute; set thresholds;
choose the route, Vision, or Mission; or return the final conclusion.
Each of these is already entailed by Canon \S7.1 (``not the final
decision maker'', ``not an execution engine'') read together with
\S8 (governance/execution separation is exhaustive: Norn is neither
governance nor execution). This section is \textbf{CLARIFIED}, not new
authority.

\chapter{Decision Procedure}
\label{ch:v73-decision-procedure}

\section{Ordered Evaluation}
\label{sec:v73-decision-procedure}

The following ordered procedure is \textbf{ORIGINAL-CONFIRMED}: it
matches the located original's \S14 ``Decision Logic,'' steps 1--10, in
identical order and content --- \textbf{including the section number
\S14 itself}. Independently reconstructed and adopted by explicit Human
Sovereign decision, 2026-09-21, before the original was located (see
\texttt{docs/V7\_3\_ADOPTION\_RECORD.md}, item 4) --- preserved as
historically valid, not superseded by this confirmation. Each
individual stage also has independent Canon or chapter grounding (cited
inline).

\begin{enumerate}
\item Vision / Mission alignment (Canon \S2.1, \S2.4; MISSION
definition, \S4/Canon).
\item Ruin boundary check (Chapter~\ref{ch:theoretical-invariants};
Chapter~\ref{ch:time-horizon-layer}; \S\ref{sec:v73-ruin-disambiguation}
below).
\item Reversibility check (R1-FIX Boundary Contract,
\S\ref{sec:r1fix-boundary-contract}).
\item $A_{\mathrm{allowed}}$ narrowing
(Chapter~\ref{ch:a-allowed}, Chapter~\ref{ch:a-allowed-dictionary}).
\item Information sufficiency check (major/irreversible lock-in is
blocked under material information insufficiency ---
\S\ref{sec:v73-a-allowed-refinement}).
\item Execution candidate generation
(\S\ref{sec:v73-strongengine-surface}).
\item Observation indicator recording (Norn,
\S\ref{ch:v73-norn}).
\item Exit condition statement.
\item Reevaluation condition/date statement.
\item Return final authority to Human Sovereign (Canon \S2.1).
\end{enumerate}

\noindent
Norn audits whether this order was followed; Norn cannot alter it
(\S\ref{sec:v73-norn-prohibitions}).

\chapter{Ruin Taxonomy}
\label{ch:v73-ruin-taxonomy}

\section{Disambiguation: Two Non-Identical Ruin Axes}
\label{sec:v73-ruin-disambiguation}

\nrmobox{ORIGINAL-CONFIRMED + CLARIFIED --- do not read as one 4-member enum}{
\textbf{Requirement (\textbf{ORIGINAL-CONFIRMED})}: the located
original requires all four concepts be tracked. \S4.1 (NRMO Core role
list) requires ``Hard Ruin / Soft Ruin identification'' and, as a
\emph{separate} bullet item, ``Active Ruin / Passive Ruin
identification''; \S13.1--13.4 individually defines all four; the
Standard Output Format (\S6) requires all four in every response. This
requirement is no longer \texttt{SOURCE-PENDING}.

\textbf{Axis semantics (\textbf{CLARIFIED}, disambiguation required)}:
read literally as one flat four-item taxonomy, this risks conflating
two chapters with \emph{different scope and different axis-meaning} ---
a risk the original's own \S4.1 phrasing already resists, since it
presents Hard/Soft and Active/Passive as two separate bulleted role
items rather than one merged list of four:
\begin{itemize}
\item \textbf{Hard Ruin / Soft Ruin} (Chapter~\ref{ch:investment-sop},
\S47--51): a \emph{severity} axis, defined specifically within the
Investment SOP domain. Hard Ruin = unrecoverable (bankruptcy,
permanent capital loss); Soft Ruin = recoverable-in-principle
strategic failure (forced Risk-Off transition).
\item \textbf{Active Ruin / Passive Ruin}
(\S\ref{sec:omega-active-vs-passive}): a \emph{causal-origin} axis,
defined domain-generally in the StrongEngine $\Omega$ Full chapter.
Active Ruin = direct collapse from reckless action; Passive Ruin =
erosion of optionality from avoidance/rigidity/adaptation failure.
\end{itemize}
These are not four siblings of one enum; they are two independent
axes --- corroborated both by the original's own two-bullet structure
and by the Canon-adjacent chapter split --- that may co-occur (e.g.\ a
Soft Ruin in the investment domain may have either an Active or a
Passive causal origin).
}

\textbf{Resolution}: trace schemas that need both axes must record
them as \texttt{ruin\_severity \(\in\) \{Hard, Soft\}} (Investment
domain only, may be \texttt{N/A} elsewhere) and
\texttt{ruin\_origin \(\in\) \{Active, Passive\}} (domain-general)
as two separate fields, never as one four-valued field. This resolves
row 6.1 in the differential table
(\texttt{docs/V7\_3\_DIFFERENTIAL\_TABLE.md}) as
\textbf{ORIGINAL-CONFIRMED + CLARIFIED} without requiring any Canon
change (Canon does not mention any of the four terms; the axis
disambiguation is entirely chapter-to-chapter documentation, not a
Canon-level distinction).

\section{Passive Ruin: Avoidability-Window Definition (v7.2.1, adopted)}
\label{sec:v73-passive-ruin-window}

This is the single strongest-evidence item in this Part: the
avoidability-window definition of Passive Ruin was already adopted in
v7.2.1, \emph{before} any v7.3 implementation work began
(\texttt{docs/v72\_1\_validation\_record.md},
\texttt{docs/v72\_implementation\_integration\_map.md} \S2--3). It is
formally carried into v7.3 as \textbf{CLARIFIED} (re-affirmed, not
newly introduced):

\begin{itemize}
\item \textbf{Leading indicator} (primary): at the moment of choice,
does the candidate action close an avoidability window that was open
in the state immediately before the choice? (\texttt{window\_open()},
\texttt{closes\_window()}, diff-in-diff against the do-nothing
baseline --- \texttt{docs/v72\_implementation\_integration\_map.md}
\S2--3.)
\item \textbf{Lagging indicator} (retained, not deleted): the legacy
streak-based signal (time spent in a degraded-state region) is kept
as a secondary, after-the-fact diagnostic, exactly as
v7.2.1 specified. It is not deleted per the ``preserve, do not erase''
guardrail (\texttt{NORMATIVE\_CANON\_ADOPTION.md} \S Guardrails).
\end{itemize}

\noindent
\textbf{Passive Ruin must not be redefined as mere inactivity.} An
implementation that flags Passive Ruin purely from an inactivity
streak, without an observed avoidability-window closure, is
non-conformant with this definition (this restates
\texttt{docs/v72\_1\_validation\_record.md} \S3--4, not a new rule).

\chapter{$A_{\mathrm{allowed}}$ and StrongEngine Refinement}
\label{ch:v73-aallowed-strongengine}

\section{Admissibility Refinement}
\label{sec:v73-a-allowed-refinement}

Building on Chapter~\ref{ch:a-allowed} and
Chapter~\ref{ch:a-allowed-dictionary} (\textbf{UNCHANGED} baseline), an
action is inadmissible (excluded from $A_{\mathrm{allowed}}$) when it:

\begin{enumerate}
\item creates direct irreversible loss beyond the applicable
reversibility floor (R1-FIX Boundary Contract,
\S\ref{sec:r1fix-boundary-contract});
\item locks in a major/high-commitment decision under material
information insufficiency;
\item removes a required withdrawal or review capability; or
\item violates a sovereignty constraint (i.e.\ would remove or bypass
the Human Sovereign's ability to accept, reject, defer, or terminate
--- Canon \S2.1).
\end{enumerate}

This is \textbf{CLARIFIED}: each clause is independently grounded in
existing chapters; their joint statement as an admissibility checklist
is new packaging, not new substance.

\section{StrongEngine Candidate Surface}
\label{sec:v73-strongengine-surface}

Within $A_{\mathrm{allowed}}$, Chapter~\ref{ch:strong-engine} and
Chapter~\ref{ch:omega-full-integrated} already establish that StrongEngine
$\Omega$ Full searches and ranks candidates without redefining the
ruin boundary. Two distinct claims must not be conflated here --- one
confirmed by the located original, one not (see
\texttt{docs/V7\_3\_ORIGINAL\_SOURCE\_DISCOVERY\_RECORD.md} \S7 for the
full layer analysis).

\paragraph{Presentation layer (\textbf{ORIGINAL-CONFIRMED}, narrowly).}
The located original's \S6 (``StrongEngine Candidates,'' under
``Standard Output Format'') and \S7 (``PP\_Deliver Format,''
original-language section headings preserved unmodified in the source
file) specify a \emph{default} response presenting three named
alternatives: A --- safe-leaning; B --- standard; C ---
aggressive/forward; all constrained within $A_{\mathrm{allowed}}$. This
is confirmed exactly as an output-presentation default (\S6's own
heading translates to ``normally respond in the following format,''
i.e.\ a default, not stated as an unconditional absolute). The original
does not separately address StrongEngine's internal candidate-
generation architecture.

\paragraph{Internal candidate generation (\textbf{DEFERRED / OUT OF
v7.3 NORMATIVE SCOPE --- NOT ADOPTED}).} A closed seven-category
minimum surface (safe/conservative; standard; assertive/forward;
information-acquisition probe; bounded sequence/checkpoint plan;
costed HOLD; exit/recovery) was proposed 2026-09-20 and \textbf{deferred,
never adopted}, 2026-09-21 (\texttt{docs/V7\_3\_ADOPTION\_RECORD.md}
\S4). A revised, open-ended principle (\emph{StrongEngine $\Omega$ Full
must not collapse candidate generation to a single risk posture when
materially distinct admissible alternatives exist within
$A_{\mathrm{allowed}}$}, ``Revision 2'') was subsequently drafted
(\texttt{docs/V7\_3\_ADOPTION\_PROPOSAL.md} \S11). \textbf{Disposition
(Human Sovereign, 2026-09-22): Revision 2 is explicitly placed outside
v7.3 normative scope and not adopted into this Part.} Neither the
seven-category text nor Revision 2 appears anywhere in either located
original file; the original's own requirement on this topic is fully
covered by the presentation-layer default above
(\S\ref{sec:v73-strongengine-surface}, ``Presentation layer''), which
is \textbf{ORIGINAL-CONFIRMED} and sufficient for v7.3 completeness.
\textbf{This disposition is not a rejection or deletion of Revision
2} --- its text is preserved in full at
\texttt{docs/V7\_3\_ADOPTION\_PROPOSAL.md} \S11 as an open proposal for
a future NRMO version or an independent track, and remains available
for reconsideration at any time. No candidate-category text beyond the
presentation-layer default above is asserted as normative v7.3 content
by this section.

\noindent
\textbf{Invariant (UNCHANGED)}: no advisory or presentation layer may
substitute an inadmissible action for the StrongEngine's selection.
This is the strict invariant $a_t \in A_t$ (Canon \S2.3, \S8) restated,
not a new rule, and is unaffected by Revision 2's scope disposition.

\chapter{Trace and DecisionRecord Schema}
\label{ch:v73-trace-schema}

\texttt{NORMATIVE\_CANON.md} \S10 requires that publication version,
baseline version, component version, and implementation-snapshot
version remain distinguishable, but does not itself enumerate a
decision-trace schema. This section's fields split into two
provenance tiers, per
\texttt{docs/V7\_3\_ORIGINAL\_SOURCE\_DISCOVERY\_RECORD.md} \S7a; they
must not be presented as one undifferentiated ``confirmed'' list.

\paragraph{Response-content fields (\textbf{ORIGINAL-CONFIRMED}).} Each
of the following is directly present in the located original's \S6
Standard Output Format:

\begin{itemize}
\item operational mode / context override / Type ZERO internal gate
(qualified per \S\ref{sec:v73-typezero-disambiguation}) / lifecycle
state / MISSION-DEFENSE flag;
\item Vision/Mission reference, where supplied by the Human Sovereign;
\item ruin results: \texttt{ruin\_severity} and \texttt{ruin\_origin}
as two separate fields (\S\ref{sec:v73-ruin-disambiguation});
\item reversibility classification;
\item admissible candidates (\S\ref{sec:v73-strongengine-surface});
\item selected action / recommendation;
\item exit conditions;
\item reevaluation conditions/date.
\end{itemize}

\paragraph{Persistence/audit trace fields (\textbf{HUMAN-SOVEREIGN-
ADOPTED TRACE EXTENSION}).} \textbf{Confirmed absent from both located
original files.} Adopted by explicit Human Sovereign decision,
2026-09-21, before the original was located (see
\texttt{docs/V7\_3\_ADOPTION\_RECORD.md}, item 6) --- this adoption
decision is the operative authority for these fields specifically, and
remains so independent of original-text status; it is not reverted to
SOURCE-PENDING merely because the original does not contain it:

\begin{itemize}
\item authority/spec version (e.g.\ \texttt{NRMO v7.3} tag) and
component versions;
\item vetoed candidates, \emph{with reasons}, as a persisted field
(the original shows only the response-time recommendation, not a
persisted admissible/vetoed-with-reasons ledger);
\item StrongEngine score metadata;
\item observation indicators, and Norn branch / procedure / drift
observations (\S\ref{ch:v73-norn}), as persisted trace fields --- the
underlying Norn responsibilities themselves are
\textbf{ORIGINAL-CONFIRMED} (\S\ref{ch:v73-norn}); their inclusion as
itemized fields in a persisted schema is this extension's own framing;
\item Human Sovereign's recorded acceptance, modification, or
rejection, as a \emph{persisted, auditable} field (the original
describes a live chat response, not a persistence/audit record of the
user's subsequent decision).
\end{itemize}

\chapter{Summary Classification and DecisionCompass Cross-Reference}
\label{ch:v73-summary}

\section{Classification Summary}

\begin{table}[ht]
\centering
\small
\begin{tabularx}{\linewidth}{lXr}
\toprule
\textbf{Classification} & \textbf{Meaning} & \textbf{Rows (differential table)} \\
\midrule
UNCHANGED & Restates existing Canon/v7.2, no delta & 17 \\
CLARIFIED & Adds precision, no new claim & 4 \\
ORIG.-CONFIRMED + CLARIFIED\textsuperscript{c} & Axis/four-concept requirement confirmed, disambiguation clarified & 2 \\
ORIGINAL-CONFIRMED & Verbatim/near-verbatim in located original & 6 \\
ADOPTED TRACE EXT.\textsuperscript{a} & No original precedent; explicitly adopted & 1 \\
DEFERRED\textsuperscript{b} & Out of v7.3 scope; not adopted, not deleted & 1 \\
SUPERSEDED & Explicitly replaces prior content & 0 \\
CONFLICT (unresolved) & None remaining & 0 \\
IMPLEMENTATION-ONLY & DecisionCompass runtime/CI concern & 2 \\
HISTORICAL & Already-non-normative material & 1 \\
\midrule
\textbf{Total} & & \textbf{34} \\
\bottomrule
\end{tabularx}
\caption[v7.3 differential classification summary]{Summary of
\texttt{NRMOIntegrated/docs/V7\_3\_DIFFERENTIAL\_TABLE.md}, updated
2026-09-21 per Human Sovereign adoption
(\texttt{docs/V7\_3\_ADOPTION\_RECORD.md}) and, same day, the
original-source discovery
(\texttt{docs/V7\_3\_ORIGINAL\_SOURCE\_DISCOVERY\_RECORD.md}); both
preserved unmodified as history. No row required a
\texttt{NORMATIVE\_CANON.md} change. \textsuperscript{a}
\texttt{HUMAN-SOVEREIGN-ADOPTED TRACE EXTENSION}.
\textsuperscript{b} \texttt{DEFERRED / OUT OF v7.3 NORMATIVE SCOPE ---
NOT ADOPTED} (Human Sovereign, 2026-09-22); not a publication blocker.
\textsuperscript{c} rows 2.3 (Type ZERO axis) and 6.1 (Ruin
four-concept tracking): axis/requirement existence
\texttt{ORIGINAL-CONFIRMED}, disambiguation discipline
\texttt{CLARIFIED} --- see \S\ref{sec:v73-typezero-disambiguation} and
\S\ref{sec:v73-ruin-disambiguation}.}
\label{tab:v73-classification-summary}
\end{table}

\section{DecisionCompass Conformance Cross-Reference}
\label{sec:v73-decisioncompass-conformance-summary}

As of DecisionCompass branch \texttt{agent/nrmo-v7-3-canonical-parity}
(HEAD \texttt{7ee15a964bce51aba22a997e9bba31a389514ac2}), the
implementation's own conformance matrix
(\texttt{docs/nrmo\_v7\_3\_conformance.json}) records all tracked
requirements as \texttt{UNIT\_VERIFIED}, \texttt{SCENARIO\_VERIFIED},
or \texttt{CI\_VERIFIED}, with one item
(\texttt{physical-device}) recorded as
\texttt{NOT\_RUN\_NO\_CONNECTED\_DEVICE} (conditional on hardware
availability, not a specification gap). This cross-reference is
informational: it describes the implementation's self-reported state
at a pinned commit and does \textbf{not} certify conformance on behalf
of NRMO, and does not make the implementation the source of this
Part's content (\texttt{NORMATIVE\_CANON.md} \S11).

\section{Non-Goals of This Part}

Restating the implementation-target document's own non-goals, which
are themselves consistent with \texttt{NORMATIVE\_CANON.md} \S9 and
\S11 (\textbf{UNCHANGED}):

\begin{itemize}
\item Experimental prior work is not adopted merely because it exists.
\item This Part does not require any particular service-extraction or
deployment architecture.
\item Historical implementation-specific task routers (e.g.\ Shinobi)
must not be conflated with canonical Norn (Canon \S7.2).
\item \textbf{NRMO theory is not rewritten for implementation
convenience} --- where this build found tension between an
implementation-target document and the Canon, the Canon prevailed
(\S\ref{sec:v73-typezero-disambiguation},
\S\ref{sec:v73-ruin-disambiguation}), and no Canon text was altered.
\end{itemize}
